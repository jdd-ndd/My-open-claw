# 强制行尾规则 - 防止 Windows 下的 CRLF/LF 互相转换产生 noise diff
#
# 经验教训:
# Git for Windows 默认 autocrlf=true，clone 时会把 LF 转 CRLF，
# 然后任何 build/test 触发文件访问都会让 Git 报 M 状态。
# 解决: 显式声明仓库统一用 LF，特殊二进制文件禁止转换。
#
# 规则:
#   * text=auto               — Git 自动判断是不是文本
#   * text eol=lf             — 文本文件统一用 LF 提交/检出
#   * binary                  — 二进制文件禁止任何转换
#   * -text                   — 排除某些已知是文本但 Git 误判的（如补丁）

# 默认所有文本 LF
* text=auto eol=lf

# 锁文件必须 LF（pnpm 严格）
pnpm-lock.yaml text eol=lf

# Windows 专属脚本
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# 文档和配置
*.md text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
*.tsbuildinfo binary

# 已知二进制
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary
*.pdf binary
*.zip binary
*.tar binary
*.gz binary

# 排除: 临时测试输出 + 编译产物
package-lock.json -text
pnpm-debug.log* -text
npm-debug.log* -text
yarn-debug.log* -text
yarn-error.log* -text
.env -text
.env.local -text
.env.*.local -text
dist/** -text
build/** -text
coverage/** -text
node_modules/** -text
server/data/** -text
*.tsbuildinfo -text
