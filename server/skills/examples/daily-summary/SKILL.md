# 每日总结技能

## 用途
自动汇总指定日期的对话记录、任务完成情况和关键信息，生成结构化日报。

## 使用场景
- 每日工作总结
- 项目进度回顾
- 学习笔记整理

## 参数
- `date` (可选): 日期，默认为今天
- `format` (可选): 输出格式 (markdown / text)，默认 markdown

## 所需工具
- `memory_search` — 检索历史对话记录
- `fs` — 将报告写入本地文件

## 示例
> 用户：帮我总结今天的工作
> Agent：调用 daily_summary(date="2026-07-21")
