# MyOpenClaw 快速入门指南

> **版本**：v1.0.2  
> **修订日期**：2026-07-23  
> **修订人**：MyOpenClaw Core Team  
> **文档状态**：正式发布（已同步代码实际状态）

---

## 目录

- [1. 文档简介](#1-文档简介)
- [2. 环境准备](#2-环境准备)
  - [2.1 Node.js 环境安装](#21-nodejs-环境安装)
  - [2.2 包管理器选择](#22-包管理器选择)
  - [2.3 系统依赖检查](#23-系统依赖检查)
- [3. 源码获取与安装](#3-源码获取与安装)
  - [3.1 克隆仓库](#31-克隆仓库)
  - [3.2 安装依赖](#32-安装依赖)
  - [3.3 构建项目](#33-构建项目)
- [4. 配置文件说明](#4-配置文件说明)
  - [4.1 配置目录结构](#41-配置目录结构)
  - [4.2 主配置文件 config.yaml](#42-主配置文件-configyaml)
  - [4.3 Agent 配置文件](#43-agent-配置文件)
  - [4.4 渠道配置文件](#44-渠道配置文件)
- [5. LLM API Key 配置](#5-llm-api-key-配置)
  - [5.1 环境变量方式](#51-环境变量方式)
  - [5.2 配置文件方式](#52-配置文件方式)
  - [5.3 密钥安全最佳实践](#53-密钥安全最佳实践)
- [6. 首个 Agent 启动](#6-首个-agent-启动)
  - [6.1 初始化系统](#61-初始化系统)
  - [6.2 创建第一个 Agent](#62-创建第一个-agent)
  - [6.3 启动 Gateway 守护进程](#63-启动-gateway-守护进程)
- [7. WebUI 访问](#7-webui-访问)
  - [7.1 访问管理面板](#71-访问管理面板)
  - [7.2 面板功能概览](#72-面板功能概览)
- [8. 通过 CLI 交互](#8-通过-cli-交互)
  - [8.1 CLI 基础命令](#81-cli-基础命令)
  - [8.2 与 Agent 对话](#82-与-agent-对话)
  - [8.3 管理命令](#83-管理命令)
- [9. 首个自动化任务示例](#9-首个自动化任务示例)
  - [9.1 任务场景说明](#91-任务场景说明)
  - [9.2 创建定时任务](#92-创建定时任务)
  - [9.3 验证任务执行](#93-验证任务执行)
- [10. 常见问题](#10-常见问题)

---

## 1. 文档简介

本指南旨在帮助开发者在 15 分钟内完成 MyOpenClaw 框架的安装、配置并启动第一个 AI Agent。MyOpenClaw 是一个本地优先（Local-First）、自托管的开源 AI Agent 框架，采用 TypeScript + Node.js 开发，具有以下核心特性：

- **本地优先**：所有数据存储在本地，隐私可控
- **自托管**：完全自主部署，无需依赖第三方云服务
- **多渠道接入**：支持 WebChat、QQBot、飞书、微信、CLI 等多种渠道
- **Hub-Spoke 架构**：六层架构设计，模块化、可扩展
- **安全沙箱**：内置安全隔离机制，防止危险操作

### 架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                    MyOpenClaw 系统架构                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Channels 渠道接入层                                        │
│  (WebChat / QQBot / 飞书 / 微信 / CLI)                      │
│        │                                                    │
│        ▼                                                    │
│  Gateway 网关控制平面 (守护进程, 端口 18780)                │
│  (WebSocket/HTTP 服务 / 消息路由 / 状态管理 / 安全沙箱)    │
│        │                                                    │
│        ▼                                                    │
│  Agent Runtime 智能体运行时                                 │
│  (LLM 推理 / 对话管理 / 工具调用编排)                      │
│        │                                                    │
│        ▼                                                    │
│  Skill / Tools 工具执行层                                   │
│  (内置工具 / 自定义技能 / 沙箱执行)                        │
│        │                                                    │
│        ▼                                                    │
│  Memory 持久存储层                                          │
│  (SQLite / 向量数据库 / 文件系统)                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 环境准备

### 2.1 Node.js 环境安装

MyOpenClaw 要求 Node.js 20 LTS 或更高版本。以下是在不同操作系统上的安装方法。

#### Ubuntu / Debian

```bash
# 添加 NodeSource 仓库并安装 Node.js 20 LTS
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装版本
node --version
# 预期输出: v20.x.x

npm --version
# 预期输出: 10.x.x
```

#### macOS (使用 Homebrew)

```bash
# 安装 Node.js 20 LTS
brew install node@20

# 将 node@20 加入 PATH（如果未自动配置）
echo 'export PATH="/opt/homebrew/opt/node@20/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# 验证安装版本
node --version
npm --version
```

#### Windows (使用 nvm-windows)

```powershell
# 使用 nvm-windows 安装 Node.js 20 LTS
nvm install 20
nvm use 20

# 验证安装版本
node --version
npm --version
```

#### 使用 nvm（跨平台推荐）

```bash
# 安装 nvm（Node Version Manager）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash

# 重新加载 shell 配置
source ~/.bashrc

# 安装并使用 Node.js 20 LTS
nvm install 20
nvm use 20
nvm alias default 20

# 验证安装
node --version
npm --version
```

### 2.2 包管理器选择

MyOpenClaw 同时支持 `npm` 和 `pnpm` 作为包管理器。推荐使用 `pnpm` 以获得更快的安装速度和更小的磁盘占用。

#### 安装 pnpm

```bash
# 方式一：使用 npm 全局安装
npm install -g pnpm

# 方式二：使用 Node.js Corepack（推荐，Node.js 16.13+ 内置）
corepack enable
corepack prepare pnpm@latest --activate

# 验证安装
pnpm --version
# 预期输出: 9.x.x
```

#### 包管理器对比

| 特性 | npm | pnpm |
|------|-----|------|
| 安装速度 | 基准 | 快 2-3 倍 |
| 磁盘占用 | 基准 | 节省 50%+ |
| 幽灵依赖 | 存在 | 不存在 |
| Monorepo 支持 | Workspaces | Workspaces（更优） |
| 推荐场景 | 简单项目 | **MyOpenClaw 推荐** |

### 2.3 系统依赖检查

在安装 MyOpenClaw 之前，请确保系统满足以下最低要求：

```bash
# 创建环境检查脚本
cat << 'EOF' > check_env.sh
#!/bin/bash

# 环境检查脚本 - 验证 MyOpenClaw 运行所需的系统依赖

echo "========== MyOpenClaw 环境检查 =========="

# 检查操作系统
echo ""
echo "[1] 操作系统信息:"
uname -a

# 检查 Node.js 版本
echo ""
echo "[2] Node.js 版本:"
NODE_VERSION=$(node --version 2>/dev/null)
if [ -z "$NODE_VERSION" ]; then
    echo "  ✗ Node.js 未安装，请先安装 Node.js 20 LTS"
    exit 1
else
    echo "  ✓ Node.js 已安装: $NODE_VERSION"
fi

# 检查 npm 版本
echo ""
echo "[3] npm 版本:"
NPM_VERSION=$(npm --version 2>/dev/null)
if [ -z "$NPM_VERSION" ]; then
    echo "  ✗ npm 未安装"
    exit 1
else
    echo "  ✓ npm 已安装: v$NPM_VERSION"
fi

# 检查 pnpm 版本（可选）
echo ""
echo "[4] pnpm 版本（可选）:"
PNPM_VERSION=$(pnpm --version 2>/dev/null)
if [ -z "$PNPM_VERSION" ]; then
    echo "  ! pnpm 未安装（推荐安装以获得更好性能）"
else
    echo "  ✓ pnpm 已安装: v$PNPM_VERSION"
fi

# 检查 Git
echo ""
echo "[5] Git 版本:"
GIT_VERSION=$(git --version 2>/dev/null)
if [ -z "$GIT_VERSION" ]; then
    echo "  ✗ Git 未安装，请先安装 Git"
    exit 1
else
    echo "  ✓ Git 已安装: $GIT_VERSION"
fi

# 检查可用内存（建议至少 2GB）
echo ""
echo "[6] 系统内存:"
if [[ "$OSTYPE" == "darwin"* ]]; then
    MEM_SIZE=$(sysctl -n hw.memsize | awk '{print $1/1024/1024/1024 " GB"}')
else
    MEM_SIZE=$(free -h | grep Mem | awk '{print $2}')
fi
echo "  总内存: $MEM_SIZE"

# 检查可用磁盘空间（建议至少 1GB）
echo ""
echo "[7] 磁盘空间:"
if [[ "$OSTYPE" == "darwin"* ]]; then
    DISK_SPACE=$(df -h / | awk 'NR==2 {print $4 " 可用"}')
else
    DISK_SPACE=$(df -h / | awk 'NR==2 {print $4 " 可用"}')
fi
echo "  根分区: $DISK_SPACE"

echo ""
echo "========== 环境检查完成 =========="
EOF

chmod +x check_env.sh
./check_env.sh
```

预期输出示例：

```
========== MyOpenClaw 环境检查 ==========

[1] 操作系统信息:
Linux hostname 5.15.0-91-generic #101-Ubuntu SMP x86_64 GNU/Linux

[2] Node.js 版本:
  ✓ Node.js 已安装: v20.11.0

[3] npm 版本:
  ✓ npm 已安装: v10.2.4

[4] pnpm 版本（可选）:
  ✓ pnpm 已安装: v9.0.0

[5] Git 版本:
  ✓ Git 已安装: git version 2.34.1

[6] 系统内存:
  总内存: 16G

[7] 磁盘空间:
  根分区: 45G 可用

========== 环境检查完成 ==========
```

---

## 3. 源码获取与安装

### 3.1 进入项目目录

```bash
# 项目已位于本地，直接进入 server 目录
cd "My open claw/server"
```

### 3.2 安装依赖

```bash
# 使用 npm 安装依赖
npm install
```

### 3.3 开发模式启动

```bash
# 开发模式（自动热重载）
npm run dev

# 或构建后启动
npm run build
npm start
```

---

## 4. 配置文件说明

### 4.1 配置目录结构

MyOpenClaw 使用 YAML 格式的配置文件，默认配置目录结构如下：

```
server/config/
├── config.yaml              # 主配置文件（Gateway/日志/安全/存储）
├── agents/                  # Agent 配置目录
│   └── default.yaml         # 默认 Agent 配置
├── channels/                # 渠道配置目录
│   ├── webchat.yaml         # WebChat 渠道配置
│   ├── qqbot.yaml           # QQBot 渠道配置
│   ├── wechat.yaml          # WeChat 渠道配置
│   └── feishu.yaml          # 飞书渠道配置
```

### 4.2 主配置文件 config.yaml

主配置文件是系统的核心配置，包含 Gateway 端口、日志级别、安全限制和存储路径等全局设置：

```yaml
include:
  - agents/default.yaml
  - channels/webchat.yaml
  - channels/qqbot.yaml
  - channels/wechat.yaml
  - channels/feishu.yaml

gateway:
  host: 127.0.0.1
  port: 18780
  heartbeatInterval: 30000
  maxConnections: 1000
  requestTimeout: 30000

logging:
  level: info

security:
  rateLimit:
    max: 100
    windowMs: 60000

storage:
  dataDir: ./data
```

### 4.3 Agent 配置文件

每个 Agent 对应一个 YAML 配置文件，存放在 `~/.myopenclaw/agents/` 目录下：

```yaml
# ~/.myopenclaw/agents/default.yaml
# 默认 Agent 配置

# Agent 唯一标识符
id: "default"
# Agent 显示名称
name: "MyOpenClaw 助手"
# Agent 描述
description: "默认的通用助手 Agent"

# LLM 模型配置
model:
  # 使用的 LLM 提供商
  provider: "openai"
  # 模型名称
  model: "gpt-4o"
  # 温度参数（0-2，越高越随机）
  temperature: 0.7
  # 最大输出 Token 数
  maxTokens: 4096
  # 系统 Prompt
  systemPrompt: |
    你是 MyOpenClaw 助手，一个功能强大的 AI Agent。
    你可以帮助用户回答问题、执行任务、管理日程等。
    请用简洁清晰的中文回答用户的问题。

# 记忆配置
memory:
  # 是否启用对话记忆
  enabled: true
  # 短期记忆窗口大小（保留最近 N 轮对话）
  shortTermWindow: 20
  # 是否启用长期语义记忆
  longTermEnabled: true
  # 长期记忆检索数量
  retrievalCount: 5

# 工具配置
tools:
  # 启用的内置工具列表
  enabled:
    - "web_search"       # 网页搜索
    - "file_read"        # 文件读取
    - "file_write"       # 文件写入
    - "shell_exec"       # 命令执行（受沙箱保护）
    - "http_request"     # HTTP 请求
  # 工具执行超时时间（毫秒）
  timeout: 30000

# 渠道绑定（指定该 Agent 响应哪些渠道的消息）
channels:
  - channelId: "webchat"
    # 路由规则：匹配特定用户
    userIds: ["*"]  # "*" 表示匹配所有用户
  - channelId: "webchat"
    userIds: ["*"]
```

### 4.4 渠道配置文件

渠道配置文件定义各消息渠道的接入参数，存放在 `~/.myopenclaw/channels/` 目录下。详细配置参见 [04-Channels渠道模块](./04-Channels渠道模块.md) 文档。

---

## 5. LLM API Key 配置

MyOpenClaw 需要配置 LLM 提供商的 API Key 才能让 Agent 进行智能推理。支持多种配置方式。

### 5.1 环境变量方式

这是最推荐的方式，API Key 不会写入配置文件，避免泄露风险。

```bash
# 创建环境变量文件
cat << 'EOF' > ~/.myopenclaw/.env

# ===== MyOpenClaw 环境变量配置 =====

# OpenAI API Key
# 获取地址: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Anthropic Claude API Key
# 获取地址: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 通义千问 API Key（阿里云百炼平台）
# 获取地址: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# DeepSeek API Key
# 获取地址: https://platform.deepseek.com/
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 智谱 GLM API Key
# 获取地址: https://open.bigmodel.cn/
ZHIPU_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

EOF

# 设置文件权限，仅当前用户可读写
chmod 600 ~/.myopenclaw/.env

# 加载环境变量（可选，MyOpenClaw 启动时会自动加载 .env 文件）
export $(cat ~/.myopenclaw/.env | xargs)
```

### 5.2 配置文件方式

也可以在主配置文件中直接配置 API Key（不推荐用于生产环境）：

```yaml
# ~/.myopenclaw/config.yaml（追加 LLM 密钥配置）

llm:
  providers:
    # OpenAI 配置
    openai:
      apiKey: "sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
      baseURL: "https://api.openai.com/v1"
    
    # Anthropic Claude 配置
    anthropic:
      apiKey: "sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
      baseURL: "https://api.anthropic.com"
    
    # 通义千问配置（阿里云）
    dashscope:
      apiKey: "sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
      baseURL: "https://dashscope.aliyuncs.com/compatible-mode/v1"
    
    # DeepSeek 配置
    deepseek:
      apiKey: "sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
      baseURL: "https://api.deepseek.com/v1"
    
    # 智谱 GLM 配置
    zhipu:
      apiKey: "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
      baseURL: "https://open.bigmodel.cn/api/paas/v4"
```

### 5.3 密钥安全最佳实践

| 实践 | 说明 | 推荐程度 |
|------|------|----------|
| 使用 `.env` 文件 | 密钥与代码分离，不纳入版本控制 | 强烈推荐 |
| 设置文件权限 `600` | 仅文件所有者可读写 | 强烈推荐 |
| 使用系统密钥管理 | macOS Keychain / Linux Secret Service | 生产推荐 |
| 环境变量注入 | 通过 CI/CD 或容器编排注入 | 生产推荐 |
| 写入 config.yaml | 密钥存在于配置文件中 | 不推荐 |
| 硬编码在代码中 | 密钥直接写在源码中 | 禁止使用 |

```bash
# 确保 .env 文件不会被 Git 追踪
echo ".env" >> ~/.myopenclaw/.gitignore
echo "*.key" >> ~/.myopenclaw/.gitignore
echo "*secrets*" >> ~/.myopenclaw/.gitignore
```

---

## 6. 首个 Agent 启动

### 6.1 初始化系统

首次使用 MyOpenClaw，需要执行初始化命令，创建配置目录和默认配置文件：

```bash
# 执行初始化命令
pnpm myopenclaw init

# 或使用 npx（如果未全局安装）
npx myopenclaw init
```

初始化过程交互示例：

```
╔══════════════════════════════════════════╗
║       MyOpenClaw 系统初始化向导            ║
╚══════════════════════════════════════════╝

[1/5] 配置目录
  配置目录将创建于: ~/.myopenclaw/
  ✓ 配置目录已创建

[2/5] 主配置文件
  ✓ config.yaml 已生成（使用默认配置）

[3/5] 默认 Agent
  请输入默认 Agent 名称 (默认: MyOpenClaw 助手): 
  ✓ 默认 Agent 配置已生成: ~/.myopenclaw/agents/default.yaml

[4/5] LLM API Key 配置
  请选择默认 LLM 提供商:
    1. OpenAI (GPT-4o)
    2. Anthropic (Claude)
    3. 通义千问 (Qwen)
    4. DeepSeek
    5. 智谱 (GLM)
  请输入选项 (1-5): 1
  请输入 OpenAI API Key: sk-proj-xxxxxxxxxxxxxxxx
  ✓ API Key 已保存到 ~/.myopenclaw/.env

[5/5] 数据库初始化
  ✓ SQLite 数据库已创建: ~/.myopenclaw/data/myopenclaw.db

═══════════════════════════════════════════
  初始化完成！
  
  下一步:
  1. 启动 Gateway:  pnpm myopenclaw start
  2. 打开 WebUI:    http://127.0.0.1:18791
  3. CLI 对话:      pnpm myopenclaw chat
═══════════════════════════════════════════
```

### 6.2 创建第一个 Agent

除了初始化时创建的默认 Agent，你可以创建自定义 Agent：

```bash
# 使用 CLI 创建新 Agent
pnpm myopenclaw agent create --name "代码助手" --model "gpt-4o"
```

或者手动创建配置文件：

```bash
# 创建代码助手 Agent 配置文件
cat << 'EOF' > ~/.myopenclaw/agents/coder.yaml

# 代码助手 Agent 配置
id: "coder"
name: "代码助手"
description: "专注于编程问题的 AI 助手"

model:
  provider: "openai"
  model: "gpt-4o"
  temperature: 0.3
  maxTokens: 8192
  systemPrompt: |
    你是一位专业的编程助手，精通多种编程语言和技术栈。
    你的职责是帮助用户解决编程问题、代码审查、架构设计等。
    回答时请提供清晰的代码示例和解释。
    使用 Markdown 格式输出代码块。

memory:
  enabled: true
  shortTermWindow: 30
  longTermEnabled: true
  retrievalCount: 5

tools:
  enabled:
    - "web_search"
    - "file_read"
    - "file_write"
    - "shell_exec"
  timeout: 60000

channels:
  - channelId: "webchat"
    userIds: ["*"]
EOF
```

列出所有已配置的 Agent：

```bash
pnpm myopenclaw agent list
```

预期输出：

```
┌──────────┬──────────────┬─────────┬────────────┐
│ Agent ID │ 名称         │ 模型    │ 状态       │
├──────────┼──────────────┼─────────┼────────────┤
│ default  │ MyOpenClaw 助手 │ gpt-4o  │ 已注册     │
│ coder    │ 代码助手     │ gpt-4o  │ 已注册     │
└──────────┴──────────────┴─────────┴────────────┘
```

### 6.3 启动 Gateway 守护进程

Gateway 是 MyOpenClaw 的核心守护进程：

```bash
# 开发模式启动
npm run dev

# 生产模式构建并启动
npm run build
npm start
```

启动成功后，预期输出：
```
[MyOpenClaw] Gateway 已启动 (端口: 18780)
```

验证 Gateway 是否正常运行：
```bash
# 检查健康状态
curl http://127.0.0.1:18780/api/health
# 返回: {"ok":true,"data":{"status":"healthy"}}

# 检查运行状态
curl http://127.0.0.1:18780/api/status
```

---

## 7. 通过 npm scripts 运行

Gateway 不含内置 WebUI，开发阶段主要通过 npm scripts 和 API 端点进行操作。

### 7.1 可用脚本

```json
{
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:e2e": "vitest run tests/e2e",
    "typecheck": "tsc --noEmit"
  }
}
```

### 7.2 常用命令

```bash
# 开发模式启动（热重载）
npm run dev

# 构建项目
npm run build

# 生产模式启动
npm start

# 运行测试
npm test

# 类型检查
npm run typecheck
```

---

## 9. 首个自动化任务示例

### 9.1 任务场景说明

在本示例中，我们将创建一个每日早间新闻摘要自动化任务。该任务每天早上 9:00 自动执行，Agent 会搜索当日科技新闻并生成摘要，然后将摘要通过 WebChat 渠道推送给用户。

### 9.2 创建定时任务

#### 方式一：使用 CLI 创建

```bash
# 创建每日早间新闻摘要任务
pnpm myopenclaw task create \
  --name "每日科技新闻摘要" \
  --cron "0 9 * * *" \
  --agent default \
  --message "请搜索今天的科技新闻，挑选最重要的5条，生成一份简洁的新闻摘要，包括标题和一句话概述。" \
  --channel webchat \
  --user-id user_001
```

预期输出：

```
✓ 定时任务创建成功

任务信息:
  ID:       task_20260721_001
  名称:     每日科技新闻摘要
  Cron:     0 9 * * * (每天 09:00 执行)
  Agent:    default
  渠道:     webchat
  用户:     user_001
  消息:     请搜索今天的科技新闻...
  状态:     已启用
  下次执行: 2026-07-22 09:00:00
```

#### 方式二：编写 TypeScript 脚本创建

对于更复杂的自动化场景，可以通过编程方式创建任务：

```typescript
// create-task.ts
// 自动化任务创建脚本 - 演示如何通过编程方式创建定时任务

// 导入 MyOpenClaw SDK 客户端
import { MyOpenClawClient } from '@myopenclaw/sdk';

/**
 * 创建每日科技新闻摘要任务的示例脚本
 * 该脚本演示了如何通过 MyOpenClaw SDK 编程式地创建定时任务
 */
async function main(): Promise<void> {
  // 创建 MyOpenClaw 客户端实例
  // 默认连接本地 ws://127.0.0.1:18780
  const client = new MyOpenClawClient({
    gatewayUrl: 'ws://127.0.0.1:18780',
    // 如有配置 API Token，需要传入
    apiToken: process.env.OPENCLAW_API_TOKEN,
  });

  try {
    // 连接到 Gateway
    await client.connect();
    console.log('已连接到 MyOpenClaw Gateway');

    // 创建定时任务
    const task = await client.task.create({
      // 任务名称
      name: '每日科技新闻摘要',
      // Cron 表达式：每天 09:00 执行
      cron: '0 9 * * *',
      // 目标 Agent ID
      agentId: 'default',
      // 任务触发时发送给 Agent 的消息
      message: '请搜索今天的科技新闻，挑选最重要的5条，生成一份简洁的新闻摘要。',
      // 结果推送的目标渠道
      channelId: 'webchat',
      // 结果推送的目标用户 ID
      userId: 'user_001',
      // 任务是否启用
      enabled: true,
      // 任务元数据（可选）
      metadata: {
        category: 'news',
        priority: 'normal',
      },
    });

    console.log('定时任务创建成功:');
    console.log(`  任务 ID: ${task.id}`);
    console.log(`  任务名称: ${task.name}`);
    console.log(`  Cron 表达式: ${task.cron}`);
    console.log(`  下次执行: ${task.nextRunAt}`);

    // 查询所有任务
    const tasks = await client.task.list();
    console.log(`\n当前共有 ${tasks.length} 个定时任务:`);
    for (const t of tasks) {
      console.log(`  - [${t.id}] ${t.name} | ${t.cron} | ${t.enabled ? '已启用' : '已禁用'}`);
    }
  } catch (error) {
    console.error('操作失败:', error);
    process.exit(1);
  } finally {
    // 断开连接
    await client.disconnect();
    console.log('\n已断开与 Gateway 的连接');
  }
}

// 执行主函数
main();
```

运行脚本：

```bash
# 使用 tsx 直接运行 TypeScript 脚本
npx tsx create-task.ts
```

### 9.3 验证任务执行

#### 手动触发任务测试

无需等待到 9:00，可以手动触发任务进行测试：

```bash
# 手动触发任务
pnpm myopenclaw task trigger --id task_20260721_001
```

预期输出：

```
[2026-07-21 10:15:00] [INFO]  手动触发任务: task_20260721_001
[2026-07-21 10:15:00] [INFO]  路由至 Agent: default
[2026-07-21 10:15:00] [INFO]  Agent [default] 开始处理任务...
[2026-07-21 10:15:01] [INFO]  调用工具: web_search("科技新闻 2026年7月21日")
[2026-07-21 10:15:05] [INFO]  搜索完成，获取 10 条结果
[2026-07-21 10:15:06] [INFO]  Agent 正在生成摘要...
[2026-07-21 10:15:12] [INFO]  任务完成，推送结果至渠道: webchat
[2026-07-21 10:15:12] [INFO]  ========================================
[2026-07-21 10:15:12] [INFO]  任务执行完成
[2026-07-21 10:15:12] [INFO]  耗时: 12.3 秒
[2026-07-21 10:15:12] [INFO]  Token 使用: 输入 1024, 输出 856
[2026-07-21 10:15:12] [INFO]  ========================================
```

#### 在 WebUI 中查看结果

1. 打开 WebUI：`http://127.0.0.1:18791`
2. 进入「对话窗口」
3. 选择 `webchat` 渠道和 `user_001` 用户
4. 即可看到 Agent 推送的新闻摘要消息

#### 查看审计日志

```bash
# 查看任务执行的审计日志
pnpm myopenclaw logs --type audit --filter "task_20260721_001" --lines 20
```

预期输出：

```
[2026-07-21 10:15:00] [AUDIT] task.trigger      task_id=task_20260721_001 source=cli
[2026-07-21 10:15:00] [AUDIT] agent.invoke      agent_id=default task_id=task_20260721_001
[2026-07-21 10:15:01] [AUDIT] tool.call         tool=web_search args={"query":"科技新闻 2026年7月21日"}
[2026-07-21 10:15:05] [AUDIT] tool.result       tool=web_search duration=4012ms success=true
[2026-07-21 10:15:06] [AUDIT] llm.request        provider=openai model=gpt-4o tokens_in=1024
[2026-07-21 10:15:12] [AUDIT] llm.response       provider=openai model=gpt-4o tokens_out=856 duration=6012ms
[2026-07-21 10:15:12] [AUDIT] channel.send       channel=webchat user=user_001 message_id=msg_001
[2026-07-21 10:15:12] [AUDIT] task.complete      task_id=task_20260721_001 duration=12340ms
```

---

## 10. 常见问题

### Q1: 启动时报端口被占用怎么办？

```bash
# 查看端口占用情况
lsof -i :18780

# 如果是之前未正常关闭的 MyOpenClaw 进程，可以强制结束
kill -9 <PID>

# 或者修改配置文件使用其他端口
# 编辑 ~/.myopenclaw/config.yaml
# gateway:
#   port: 18790  # 修改为其他可用端口
```

### Q2: LLM API 调用超时怎么办？

```yaml
# 在 config.yaml 中调整超时和重试配置
llm:
  timeout: 180000    # 增加超时时间到 3 分钟
  maxRetries: 5      # 增加重试次数
```

### Q3: 如何查看详细的调试日志？

```bash
# 启动时设置日志级别为 debug
pnpm myopenclaw start --log-level debug

# 或在配置文件中修改
# logging:
#   level: "debug"
```

### Q4: 如何完全重置系统？

```bash
# 停止 Gateway
pnpm myopenclaw stop

# 备份后删除配置目录
mv ~/.myopenclaw ~/.myopenclaw.backup.$(date +%Y%m%d)

# 重新初始化
pnpm myopenclaw init
```

### Q5: 如何更新到最新版本？

```bash
# 拉取最新代码
cd ~/myopenclaw
git pull origin main

# 安装依赖
pnpm install

# 重新构建
pnpm build

# 重启 Gateway
pnpm myopenclaw restart
```

---

## 下一步

完成了快速入门后，建议继续阅读以下文档深入了解 MyOpenClaw：

- [03-Gateway网关模块](./03-Gateway网关模块.md) - 了解系统中枢的详细设计与配置
- [04-Channels渠道模块](./04-Channels渠道模块.md) - 学习如何接入和管理多种消息渠道
- Agent Runtime 文档 - 深入了解 Agent 运行时的原理和配置
- 工具开发指南 - 学习如何为 Agent 开发自定义工具和技能

---

> **注意**：当前版本中，上层业务模块（Agents Agent 运行时、Tools 工具层、Channels 渠道适配、Memory 记忆层、Skills 技能系统）仍为骨架实现。Gateway 网关层和 Core 公共模块已完成核心功能开发。详细实现状态请参阅 [00-项目总览](./00-项目总览.md)。
